import LandscapeTree from '@/components/LandscapeTree';

export const metadata = {
  title: 'AI Model Landscape — NextGen AI Directory',
  description: 'Companies, Model Categories & Model Families hierarchy.',
};

export default function LandscapePage() {
  return <LandscapeTree />;
}
